---
id: TWA-001-arch-summary
title: TWA-001 前置架构说明汇总
owner: yuan-control
status: done
priority: P0
progress: 100
---

# TWA-001 前置架构说明汇总

## 结论

用户已最终确认采纳 deep research 技术路线，1–10 项用户确认已完成。TWA-001 的技术栈冻结结论调整为：Web 主线使用 Next.js 16 + React 19，后端使用 Node.js 24 LTS + NestJS + Fastify adapter，数据与任务层使用 PostgreSQL + Drizzle ORM + Redis + BullMQ，工程底座使用 pnpm workspace + Turborepo + Biome + Zod + Storybook + MSW + Playwright。

技术冻结与用户最终确认已经完成，frontend 放行条件已满足，当前进入 TWA-001 第二阶段 App Shell / 首页 / 工作台基础静态实现。仍禁止真实登录、真实短信、真实生图、真实试卷生成、真实支付、真实额度扣费、真实 API handler、数据库 schema / migration、secret / token / key。

## 冻结技术栈

| 层 | 冻结选择 |
|---|---|
| Web 前端 | Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 + shadcn/ui + TanStack Query + React Hook Form + Zod |
| API 后端 | Node.js 24 LTS + NestJS + Fastify adapter + OpenAPI |
| 数据与任务 | PostgreSQL 17/18 + Drizzle ORM + Redis + BullMQ |
| AI 接口层 | provider adapter + Responses API + Structured Outputs |
| 工程化 | pnpm workspace + Turborepo + Biome + Storybook + MSW + Playwright |
| 移动端后续 | Expo + React Native + Expo Router + NativeWind，独立 UI，共享领域层 |

## 采纳原因

- 更适合 AI 协同开发：有明确目录约定、版本匹配文档、AGENTS.md、MCP / LLM 文档入口和自动化验证链路。
- 设计稿体现的是表单驱动的生成型业务台，需要成熟的表单、状态、上传、预览、生成任务、额度、导出和移动端差异化导航能力。
- Next.js App Router 更适合作为长期 Web 主线，Vite SPA 降为备选。
- NestJS + Fastify adapter 提供更稳定的模块、控制器、服务、DI、验证、OpenAPI 组织，对 AI 更可预测。
- PostgreSQL + Drizzle 仍是主数据库与 ORM 方向。
- Redis + BullMQ 作为生成任务队列目标栈，但不在本轮落地。
- Web 先行，移动端后续用 Expo / React Native 独立 app 线；共享 contract、API client、auth、design token 和 domain logic，不强求共享复杂 UI。

## 下一步实施原则

1. 先把 TWA-001 事实源同步为 deep research 路线。
2. 后续如进入工程落地，应先创建“工程底座 / monorepo / Next.js + NestJS scaffold”类任务。
3. 实现任务必须由干活 profile 通过 Codex 执行。
4. 不允许在本轮文档同步时安装依赖、重构前端、创建 API handler 或改数据库 schema。

## 推荐后续任务顺序

采用 B+ 顺序，并按 deep research 调整命名：

1. TWA-001：技术栈冻结 + 移动优先 App Shell / 首页 / 工作台基础
2. TWA-002：登录与会话 Mock / Adapter
3. TWA-003：额度账本 Mock / Adapter
4. TWA-004：营销生图工作台 Mock 流程
5. TWA-005：试卷生成工作台 Mock 流程
6. TWA-006：我的作品 / 生成记录
7. TWA-007：账户 / 商业化占位 + 移动端体验回归

## 硬边界

- 不实现 UI。
- 不安装依赖。
- 不创建真实 API。
- 不创建数据库迁移。
- 不接短信。
- 不接 AI 模型。
- 不接支付。
- 不做真实额度扣费。
- 不写 secret / token。
- 不修改 dashboard schema。

## Frontend 放行结论

- 用户最终确认已完成。
- deep research 路线已被正式采用。
- B+ 顺序已正式生效。
- TWA-001 owner 已切换为 `yuan-frontend`。
- 当前进入实现阶段：移动端优先 App Shell / 首页 / 工作台基础静态实现。

## MinIO 与试卷参考仓库补充确认

- MinIO 作为上传素材、生成结果、图片、PDF、Word、作品文件的对象存储层。
- MinIO 同时作为试卷参考仓库原始文件存储层。
- PostgreSQL 保存试卷元数据。
- 后续使用 pgvector / 全文索引做检索。
- AI 生成试卷前，后端必须先检索同年级、同学科、同教材版本、同考试类型的至少 3 张参考试卷，再开始生成。
- TWA-001 当前只冻结架构方向，不实现真实 OCR / embedding / 检索。
